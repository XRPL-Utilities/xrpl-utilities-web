# Cloudflare _redirects.
#
# Empty in Workers static-assets mode. The www.xrpl-utilities.com -> apex
# 301 used to live here but Workers rejects absolute URLs in _redirects
# (validation error 10021) - the same rule that broke every deploy from
# the 2026-04-26 Workers-mode switch through 2026-05-14. The redirect is
# now handled in _worker.js at the top of the fetch handler.
